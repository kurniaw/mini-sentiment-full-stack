# Leave blank to run in standalone (React state only) mode.
# Set to /api to proxy through Next.js rewrites to a local NestJS backend (localhost:3001).
# Set to https://your-api.example.com for production with a deployed backend.
NEXT_PUBLIC_API_URL=
